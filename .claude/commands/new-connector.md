Follow the **Veza OAA Agent** workflow defined in CLAUDE.md.

Arguments provided by the user: $ARGUMENTS

- If arguments describe the system and data source clearly enough, skip Step 1 and go directly to Step 2.
- If arguments are vague or incomplete, start at Step 1 (Gather Requirements).
- Always run the OAA Dry-Run Tester workflow (Mode A) after generating artifacts before reporting done.
